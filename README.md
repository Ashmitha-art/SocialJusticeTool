To run backend, navigate to Back-End folder and run command - python manage.py runserver
To run frontend, navigate to front-end folder and run command - npm run start
You need to create virtual env with the command - conda init envname
Run pip install to install libraries/dependencies
